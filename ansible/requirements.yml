# Collections required by the CloudVault playbooks.
# Install on the control node:
#   ansible-galaxy collection install -r requirements.yml
---
collections:
  - name: ansible.posix          # provides ansible.posix.synchronize
